#include<iostream>
using namespace std;
int main()
{
    return 0;
}

//hash function : hash code + compression function
//so basically suppose we want to add ek naam like mera naam ham bucket array mai store karenge so for the conversion of the string or truly any other type like char or obj we use the hash code to convert them to int so that they can be stored into the bucket array
//integer jo hash code se mila hai usko range mai lane ka kaam compression function ka hota hai